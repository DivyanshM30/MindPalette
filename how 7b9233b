[1mdiff --git a/.gitignore b/.gitignore[m
[1mindex 75d87e7..f01054a 100644[m
[1m--- a/.gitignore[m
[1m+++ b/.gitignore[m
[36m@@ -48,3 +48,9 @@[m [mcompleted_progress.md[m
 SUPABASE_UPDATE_INSTRUCTIONS.md[m
 GITHUB_SETUP.md[m
 GITHUB_PUSH_STEPS.md[m
[32m+[m[32mfull_audit.md[m
[32m+[m[32mSECURITY_AUDIT.md[m
[32m+[m[32mFEATURES_ROADMAP.md[m
[32m+[m
[32m+[m[32m# Original uncompressed favicon backup[m
[32m+[m[32mapp/favicon_original.ico[m
[1mdiff --git a/app/favicon.ico b/app/favicon.ico[m
[1mindex 50f8bc8..f5539b5 100644[m
Binary files a/app/favicon.ico and b/app/favicon.ico differ
[1mdiff --git a/components/DayView.tsx b/components/DayView.tsx[m
[1mindex ae61208..dc2b03e 100644[m
[1m--- a/components/DayView.tsx[m
[1m+++ b/components/DayView.tsx[m
[36m@@ -1,5 +1,5 @@[m
 'use client'[m
[31m-import { useState, useEffect, useCallback } from 'react'[m
[32m+[m[32mimport { useState, useEffect, useCallback, useRef } from 'react'[m
 import { motion, AnimatePresence } from 'framer-motion'[m
 import { supabase } from '@/lib/supabase'[m
 import { Mood, MoodGrade } from '@/lib/types'[m
[36m@@ -19,6 +19,9 @@[m [mexport default function DayView() {[m
     const [showCalendar, setShowCalendar] = useState(false)[m
     const [showMoodSelector, setShowMoodSelector] = useState(false)[m
 [m
[32m+[m[32m    // In-session drafts keyed by date (YYYY-MM-DD): keeps typed notes that[m
[32m+[m[32m    // can't be persisted yet (no mood selected) from being lost on navigation.[m
[32m+[m[32m    const draftsRef = useRef<Record<string, { note: string, positive: string }>>({})[m
 [m
     const { showToast } = useToast()[m
 [m
[36m@@ -42,7 +45,9 @@[m [mexport default function DayView() {[m
                     positive: data.positive_note || ''[m
                 })[m
             } else {[m
[31m-                setMoodData({ mood: null, note: '', positive: '' })[m
[32m+[m[32m                // No saved entry — restore any in-session draft for this date[m
[32m+[m[32m                const draft = draftsRef.current[dateStr][m
[32m+[m[32m                setMoodData({ mood: null, note: draft?.note || '', positive: draft?.positive || '' })[m
             }[m
 [m
             // Also fetch all moods for calendar display[m
[36m@@ -93,6 +98,7 @@[m [mexport default function DayView() {[m
                     }, { onConflict: 'user_id,date' })[m
 [m
                 if (error) throw error[m
[32m+[m[32m                delete draftsRef.current[dateStr][m
                 showToast('Mood saved successfully! ✨')[m
             }[m
         } catch (error) {[m
[36m@@ -103,26 +109,37 @@[m [mexport default function DayView() {[m
         }[m
     }, [user, selectedDate, showToast])[m
 [m
[32m+[m[32m    // If the user typed text but hasn't picked a mood, it can't be saved yet —[m
[32m+[m[32m    // let them know it's kept as a draft when they navigate away.[m
[32m+[m[32m    const warnIfUnsavedDraft = useCallback(() => {[m
[32m+[m[32m        if (moodData && !moodData.mood && (moodData.note.trim() || moodData.positive.trim())) {[m
[32m+[m[32m            showToast('Note kept as a draft — pick a mood to save it', 'info')[m
[32m+[m[32m        }[m
[32m+[m[32m    }, [moodData, showToast])[m
[32m+[m
     const handleDateChange = (newDate: Date) => {[m
[32m+[m[32m        warnIfUnsavedDraft()[m
         setSelectedDate(newDate)[m
         setShowCalendar(false)[m
     }[m
 [m
     const handlePrevDay = useCallback(() => {[m
[32m+[m[32m        warnIfUnsavedDraft()[m
         setSelectedDate(prev => {[m
             const d = new Date(prev)[m
             d.setDate(d.getDate() - 1)[m
             return d[m
         })[m
[31m-    }, [])[m
[32m+[m[32m    }, [warnIfUnsavedDraft])[m
 [m
     const handleNextDay = useCallback(() => {[m
[32m+[m[32m        warnIfUnsavedDraft()[m
         setSelectedDate(prev => {[m
             const d = new Date(prev)[m
             d.setDate(d.getDate() + 1)[m
             return d[m
         })[m
[31m-    }, [])[m
[32m+[m[32m    }, [warnIfUnsavedDraft])[m
 [m
     const handleMoodSelect = useCallback(async (mood: MoodGrade) => {[m
         setMoodData(prev => prev ? { ...prev, mood } : { mood, note: '', positive: '' })[m
[36m@@ -131,7 +148,13 @@[m [mexport default function DayView() {[m
     }, [moodData, saveData])[m
 [m
     const handleNoteChange = (field: 'note' | 'positive', value: string) => {[m
[31m-        setMoodData(prev => prev ? { ...prev, [field]: value } : { mood: null, note: '', positive: '', [field]: value })[m
[32m+[m[32m        const base = moodData ?? { mood: null, note: '', positive: '' }[m
[32m+[m[32m        const next = { ...base, [field]: value }[m
[32m+[m[32m        if (!next.mood) {[m
[32m+[m[32m            // Not persistable yet — keep as an in-session draft for this date[m
[32m+[m[32m            draftsRef.current[selectedDate.toLocaleDateString('en-CA')] = { note: next.note, positive: next.positive }[m
[32m+[m[32m        }[m
[32m+[m[32m        setMoodData(next)[m
     }[m
 [m
     // Keyboard shortcuts: ← → navigate days, 1-5 select moods, T = today[m
[36m@@ -145,12 +168,12 @@[m [mexport default function DayView() {[m
 [m
             if (e.key === 'ArrowLeft') { e.preventDefault(); handlePrevDay() }[m
             else if (e.key === 'ArrowRight') { e.preventDefault(); handleNextDay() }[m
[31m-            else if (e.key.toLowerCase() === 't') { setSelectedDate(new Date()) }[m
[32m+[m[32m            else if (e.key.toLowerCase() === 't') { warnIfUnsavedDraft(); setSelectedDate(new Date()) }[m
             else if (moodKeys[e.key]) { handleMoodSelect(moodKeys[e.key]) }[m
         }[m
         window.addEventListener('keydown', handleKeyboard)[m
         return () => window.removeEventListener('keydown', handleKeyboard)[m
[31m-    }, [showCalendar, handlePrevDay, handleNextDay, handleMoodSelect])[m
[32m+[m[32m    }, [showCalendar, handlePrevDay, handleNextDay, handleMoodSelect, warnIfUnsavedDraft])[m
 [m
     const handleSave = async () => {[m
         if (!moodData?.mood) return[m
[36m@@ -209,7 +232,7 @@[m [mexport default function DayView() {[m
                         animate={{ opacity: 1, scale: 1 }}[m
                         whileHover={{ scale: 1.05 }}[m
                         whileTap={{ scale: 0.95 }}[m
[31m-                        onClick={() => setSelectedDate(new Date())}[m
[32m+[m[32m                        onClick={() => { warnIfUnsavedDraft(); setSelectedDate(new Date()) }}[m
                         className="ml-2 px-4 py-2 rounded-xl bg-purple-100 dark:bg-purple-900/30 text-purple-600 dark:text-purple-400 text-sm font-bold shadow-sm hover:shadow-md transition-all border border-purple-200 dark:border-purple-800"[m
                     >[m
                         Today[m
[36m@@ -365,4 +388,3 @@[m [mexport default function DayView() {[m
         </div>[m
     )[m
 }[m
[31m-[m
[1mdiff --git a/components/MoodDialog.tsx b/components/MoodDialog.tsx[m
[1mindex e5781c6..b5aaaad 100644[m
[1m--- a/components/MoodDialog.tsx[m
[1m+++ b/components/MoodDialog.tsx[m
[36m@@ -16,10 +16,12 @@[m [minterface MoodDialogProps {[m
 export default function MoodDialog({ isOpen, onClose, onSelect, date, currentMood, currentNote }: MoodDialogProps) {[m
     const sortedMoods = Object.entries(MOODS) as [MoodGrade, typeof MOODS[MoodGrade]][][m
     const [localNote, setLocalNote] = useState(currentNote || '')[m
[32m+[m[32m    const [showMoodHint, setShowMoodHint] = useState(false)[m
 [m
     // Sync local note when dialog opens or currentNote changes[m
     useEffect(() => {[m
         setLocalNote(currentNote || '')[m
[32m+[m[32m        setShowMoodHint(false)[m
     }, [currentNote, isOpen])[m
 [m
     // Prevent click propagation to overlay[m
[36m@@ -42,8 +44,14 @@[m [mexport default function MoodDialog({ isOpen, onClose, onSelect, date, currentMoo[m
     const handleSaveAndClose = () => {[m
         if (currentMood) {[